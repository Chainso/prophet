// Code generated by prophet-cli. DO NOT EDIT.

import { Router, type Request, type Response, type NextFunction } from 'express';
import { createEventId } from '@prophet-ontology/events-runtime';
import { ActionExecutionService } from './action-service.js';
import type { ActionContext } from './action-handlers.js';
import * as Schemas from './validation.js';

export function buildActionRouter(
  service: ActionExecutionService,
  context: ActionContext,
): Router {
  const router = Router();

  router.post('/actions/approveOrder', async (req: Request, res: Response, next: NextFunction) => {
    const parsed = Schemas.ApproveOrderCommandSchema.safeParse(req.body ?? {});
    if (!parsed.success) {
      res.status(400).json({ error: 'invalid_request', details: parsed.error.format() });
      return;
    }
    try {
      const requestContext: ActionContext = {
        ...context,
        traceId: String(req.header('x-trace-id') ?? context.traceId ?? createEventId()),
      };
      const output = await service.approveOrder(parsed.data, requestContext);
      res.json(output);
    } catch (error) {
      next(error);
    }
  });

  router.post('/actions/createOrder', async (req: Request, res: Response, next: NextFunction) => {
    const parsed = Schemas.CreateOrderCommandSchema.safeParse(req.body ?? {});
    if (!parsed.success) {
      res.status(400).json({ error: 'invalid_request', details: parsed.error.format() });
      return;
    }
    try {
      const requestContext: ActionContext = {
        ...context,
        traceId: String(req.header('x-trace-id') ?? context.traceId ?? createEventId()),
      };
      const output = await service.createOrder(parsed.data, requestContext);
      res.json(output);
    } catch (error) {
      next(error);
    }
  });

  router.post('/actions/shipOrder', async (req: Request, res: Response, next: NextFunction) => {
    const parsed = Schemas.ShipOrderCommandSchema.safeParse(req.body ?? {});
    if (!parsed.success) {
      res.status(400).json({ error: 'invalid_request', details: parsed.error.format() });
      return;
    }
    try {
      const requestContext: ActionContext = {
        ...context,
        traceId: String(req.header('x-trace-id') ?? context.traceId ?? createEventId()),
      };
      const output = await service.shipOrder(parsed.data, requestContext);
      res.json(output);
    } catch (error) {
      next(error);
    }
  });

  return router;
}
