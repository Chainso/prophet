// GENERATED FILE: do not edit directly.
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = env("DATABASE_PROVIDER")
  url      = env("DATABASE_URL")
}

model Order {
  order_id String @id
  customer_user_id String
  total_amount Decimal
  discount_code String?
  tags String[]?
  shipping_address Json?
  current_state String @default("created")
  customer User @relation(fields: [customer_user_id], references: [user_id])
}

model User {
  user_id String @id
  email String
}
