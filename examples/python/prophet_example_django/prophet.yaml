schema_version: 1
project:
  name: prophet_example_django
  ontology_file: ontology/local/main.prophet
  version_source: ontology
generation:
  stack:
    id: python_django_django_orm
  targets:
    - sql
    - openapi
    - python
    - django
    - django_orm
    - manifest
  out_dir: gen
compatibility:
  baseline_ir: .prophet/baselines/main.ir.json
  strict_enums: false
determinism:
  canonical_sort: true
  include_timestamps: false
